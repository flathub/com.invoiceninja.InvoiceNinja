# yaml-language-server: $schema=https://raw.githubusercontent.com/flatpak/flatpak-builder/main/data/flatpak-manifest.schema.json

---
app-id: com.invoiceninja.InvoiceNinja
runtime: org.freedesktop.Platform
runtime-version: "23.08"
sdk: org.freedesktop.Sdk
command: invoiceninja
separate-locales: false
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --socket=pulseaudio
  - --share=network
  - --filesystem=xdg-pictures
  - --filesystem=xdg-documents
  - --filesystem=xdg-download
modules:
  - name: Invoice-Ninja
    buildsystem: simple
    only-arches:
      - x86_64
    build-commands:
      - "./build-flatpak.sh"
    sources:
      - type: file
        url: https://github.com/invoiceninja/admin-portal/releases/download/v5.0.151/Invoice-Ninja-Linux-Portable.tar.gz
        sha256: c5928e34551f71bffd12346c2fc7057e1891a35a921f4c0bf258c2343dade1bf
      - type: git
        url: https://github.com/invoiceninja/admin-portal.git
        tag: v5.0.151
      - type: file
        path: build-flatpak.sh